interface:
  display_name: "VPS TCP Tuning"
  short_description: "Evidence-based VPS TCP tuning workflow"
  default_prompt: "Use $vps-tcp-tuning to inspect and tune my relay or landing VPS networking safely."
policy:
  allow_implicit_invocation: true
